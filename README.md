# SE3_DayAheadLoadForecast
Goal of the repo : A day-ahead hourly electricity load forecast for bidding zone SE3 that runs unattended every day, publishes its prediction before the fact, and records its own error against what actually happened.
